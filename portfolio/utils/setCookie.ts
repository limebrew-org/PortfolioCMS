import { serialize } from 'cookie'

const serializeHttpOnlyCookieMany = (access_token:string, refresh_token:string) => {
    const serializedToken = serialize(
        
    )
}

const serializeHttpOnlyCookieOne = (access_token:string) => {
    
}